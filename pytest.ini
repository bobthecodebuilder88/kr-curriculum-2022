[pytest]
# 이 파일이 없으면 `pytest tests/` 가 conftest 를 읽다 ModuleNotFoundError 로 죽는다.
# pytest 가 sys.path 에 넣는 것은 테스트 폴더지 리포 루트가 아니라서 `pipeline` 을
# 못 찾는다. `python3 -m pytest` 는 cwd 가 들어가 우연히 되던 것뿐이고, 처음 온
# 사람은 대개 맨 앞의 `python3 -m` 을 빼고 친다.
pythonpath = .
testpaths = tests
