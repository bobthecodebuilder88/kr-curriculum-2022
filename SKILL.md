---
name: kr-curriculum-2022
description: 2022 개정 교육과정 성취기준 3294건(초·중·고 전 교과)의 코드·진술문·성취수준(A~E) 데이터베이스와 인용 검증 CLI. Use when writing or reviewing Korean K-12 lesson plans, 수업지도안, 평가계획, assessment rubrics, textbook or edtech content that cites 성취기준 codes like [9수01-01] or [10공수1-01-01] — recalled codes and statements are routinely fabricated, so look them up here instead of writing them from memory. 교육과정 연결·성취기준 인용·기존 문서의 인용 검증이 필요할 때 사용.
license: MIT (코드) / 공공누리 교육부 고시 (데이터)
---

# kr-curriculum-2022 — 성취기준은 기억이 아니라 조회다

2022 개정 교육과정 성취기준 **3294건**(초 611 · 중 704 · 고 1979)의 코드·진술문·
성취수준(A~E). 데이터는 `data/<학교급>/<교과>.jsonl` 36개 파일, 목록은 `data/index.json`.

## 절대 규칙

1. **기억으로 쓰지 마라.** 코드도 진술문도 `scripts/lookup.py` 조회 결과만 인용한다.
   그럴듯한 코드를 지어내는 것이 이 스킬이 막으려는 사고다. 조회하지 않고 쓴 코드가
   문서에 하나라도 있으면 그 문서는 검증되지 않은 것이다.
2. **진술문은 한 글자도 바꾸지 마라.** 조사·어미·띄어쓰기까지 그대로 옮긴다.
   요약·의역이 필요하면 원문을 먼저 인용하고 그 뒤에 따로 쓴다.
3. **코드만으로 성취기준을 특정하지 마라. 고유키는 `(교과, 코드)`다.**
   같은 코드가 서로 다른 두 교과에 실재하는 자리가 **10개** 있다. 교과를 확인하지 않으면
   엉뚱한 교과의 문장을 인용한다.
4. **진술문이 없으면 비워 두고 알려라 — 절대 채우지 마라.** 코드는 실재하지만 원문
   PDF 변환에서 문장이 파괴돼 수록하지 못한 레코드가 **39건**이다. 조회 결과가
   `진술문 미수록`이면 그 자리에 문장을 만들어 넣지 말고, 사용자에게 원문 고시본
   확인이 필요하다고 알려라. **이 파일에서 가장 중요한 규칙이다.**
5. **"없다"에는 두 가지 뜻이 있다.** 실재하지 않는 코드인지, 원문 손상으로 이 데이터셋에
   못 실은 코드인지 구별해서 답하라 (→ [「없다」의 두 가지 의미](#없다의-두-가지-의미)).
6. **산출물을 내기 전에 `scripts/verify.py`로 자기 검증한다.** 성취기준이 인용된 문서를
   만들었다면 마지막 단계는 항상 검증이다.

## 조회

```bash
python3 scripts/lookup.py --school 중 --subject 수학 --keyword 소인수분해 --format md
python3 scripts/lookup.py --code 9수01-01              # 코드를 아는 경우 확인용(JSON)
python3 scripts/lookup.py --school 고 --course "화법과 언어" --format md   # 공백 있는 값은 따옴표
```

첫 명령의 실제 출력:

```
- **[9수01-01]** 소인수분해의 뜻을 알고, 자연수를 소인수분해 할 수 있다. (중·수학·수학) — 교차검증됨·별책
  - A수준: 소인수분해의 뜻을 설명하고, 자연수를 소인수분해 할 수 있다.
  - C수준: 소인수분해의 뜻을 알고, 자연수를 소인수의 곱으로 표현할 수 있다.
  - E수준: 소인수를 알고, 안내된 절차에 따라 자연수를 소인수의 곱으로 표현할 수 있다.
- **[9수01-02]** 소인수분해를 이용하여 최대공약수와 최소공배수를 구할 수 있다. (중·수학·수학) — 교차검증됨·별책
  - A수준: 소인수분해를 이용하여 최대공약수와 최소공배수를 구하고 그 원리를 설명할 수 있다.
  - C수준: 소인수분해를 이용하여 두 수의 최대공약수와 최소공배수를 구할 수 있다.
  - E수준: 소인수분해 된 두 수의 최대공약수 또는 최소공배수를 구할 수 있다.
```

`--format md` 줄 끝의 `— 교차검증됨·별책`이 **신뢰 등급**이다. 그냥 넘기지 마라.
딸려 나오는 `A수준: …` 줄이 성취수준이다. 이 성취기준은 A·C·E 세 등급뿐이고, 없는 B·D를
지어 채우면 `verify.py`가 `LVLMISS`로 잡는다. 이 `A수준: …` 표기가 곧 `verify.py`가
되읽는 형식이라, 조회 결과를 그대로 옮기면 검증을 통과한다.
필터는 `--code / --school(초·중·고) / --subject / --course / --keyword` 조합.
조회 결과가 없으면 안내문과 함께 exit 1.

**`--code`는 확인용이지 출발점이 아니다.** 주제를 보고 코드가 먼저 떠올랐다면 그건 조회가
아니라 기억이다. `--keyword`·`--course`·`--subject`로 주제에서 출발해 찾아라. `--code`는
사용자가 준 코드나 기존 문서에 있는 코드를 확인할 때만 쓴다. 떠올린 코드가 실재하더라도
그 수업 주제의 성취기준이라는 보장은 없다 — `lookup.py`는 실재 여부만 답한다.

스크립트를 못 쓰는 환경이면 `data/index.json`의 경로를 보고 JSONL을 직접 grep한다.

인용 형식: `[9수01-01] 소인수분해의 뜻을 알고, 자연수를 소인수분해 할 수 있다.`
출처 표기가 필요하면 레코드의 `source` 필드를 쓴다.

### 성취수준(`levels`)도 인용문이다

평가 콘텐츠라면 `levels`(A~E)를 함께 쓴다. 3294건 중 **3113건**에 성취수준이 붙어 있고
**181건**에는 없다. 등급 구성은 레코드마다 다르다(A~E 5단계가 2024건, A~C 3단계가 858건 등).

- 없는 등급을 만들어 채우지 마라. A~C만 있는 성취기준에 D·E를 지어내는 것은 규칙 1 위반이다.
- 성취수준 서술문도 **verbatim**이다. 진술문과 똑같이 한 글자도 바꾸지 않는다.
- **`verify.py`가 성취수준도 검사한다.** `A수준: …`처럼 등급을 밝히고 콜론을 찍은 자리는
  그 등급의 서술문을 인용하겠다는 선언으로 읽고, 원문과 다르면 `LVLDIFF`, 그 성취기준에
  없는 등급이면 `LVLMISS`, 성취수준이 수록되지 않은 성취기준이면 `LVLNONE`을 낸다.
  콜론 없는 표기(`A등급 (참고) …`)는 인용 선언이 아니라 목록으로 보고 건드리지 않는다.
- 다만 `statement_verified`는 **진술문만** 대조한 값이다. 성취수준 서술문에는 그런 교차검증
  등급이 없다 — `verify.py`가 대조하는 상대는 고시 원문이 아니라 이 데이터셋의 `levels`다.
- 원문에서 그대로 찾을 수 없어 등급 배정이 틀렸을 수 있는 서술문이 **56건** 있다
  (`validation-report.md`의 「원문 대조 실패 성취수준 서술문」에 코드·등급까지 나열돼 있다).
  그 자리는 `verify.py`가 통과시켜도 원문 성취수준표를 확인해야 한다.

## 같은 코드, 다른 교과 (규칙 3)

```bash
$ python3 scripts/lookup.py --code 12심독01-01 --format md
# 주의: [12심독01-01]는 교과가 다른 성취기준 2개에 실재한다. 대상 교과를 확인하고 골라 쓰라.
- **[12심독01-01]** 다양한 분야의 기초 학문 주제에 관한 글을 읽고 주요 내용을 파악한다. (고·영어·심화 영어 독해와 작문) — 교차검증됨·별책
  … (A~E 수준 5줄 — 길어서 이 문서에서만 줄였다. 실제 출력에는 그대로 나온다)
- **[12심독01-01]** 날말이나 구, 문장을 듣고 의미를 이해한다. (고·제2외국어·심화 독일어) — 단일 출처(교차검증 불가)·별책
  - 성취수준 미수록 — 원문 성취수준표 확인 필요 (등급을 지어내지 말 것)
```

한쪽에만 성취수준이 붙어 있다는 것까지 교과를 가르는 단서다. 성취수준이 없는 쪽에
등급을 인용하면 `verify.py`가 `LVLNONE`으로 알린다(경고, exit code는 그대로).

충돌하는 코드는 정확히 이 10개다. 나머지 코드는 교과가 하나뿐이다.

- 체육 「스포츠 문화」 ↔ 제2외국어 「스페인어권 문화」: `12스문01-01` `12스문01-02` `12스문01-03`
- 영어 「심화 영어 독해와 작문」 ↔ 제2외국어 「심화 독일어」: `12심독01-01` `12심독01-02`
  `12심독01-03` `12심독01-04` `12심독02-01` `12심독02-02` `12심독02-04`
  (`12심독02-03`은 영어에만 있다 — 접두어가 같다고 전부 충돌하는 것이 아니다)

`lookup.py`는 이런 코드에 경고를 붙이지만(JSON 출력에서는 stderr), 결과가 2건 이상이면
**교과를 보고 직접 골라야 한다.** 하나를 임의로 고르지 말고, 어느 교과인지 모르면 사용자에게 물어라.

## 진술문이 없는 레코드 (규칙 4)

```bash
$ python3 scripts/lookup.py --code 12독회01-01 --format md
- **[12독회01-01]** 진술문 미수록 — 원문 고시본 확인 필요 (지어내지 말 것) (고·제2외국어·독일어 회화) — 원문 미확보
  - 성취수준 미수록 — 원문 성취수준표 확인 필요 (등급을 지어내지 말 것)
```

`statement: null` 39건. 코드는 실재하고 과목도 아는데 문장만 없다. 별책 쪽 문장이 손상된
191자리 중 152자리는 성취수준 문서에서 같은 문장을 찾아 복구했고(`statement_source:
"성취수준표"`), 끝내 복구하지 못한 나머지가 이 39건이다. 이때 할 일:

- 인용문을 쓰지 않는다. **비슷한 다른 성취기준의 문장을 가져다 붙이지 않는다.**
- **그 성취기준의 문장을 알고 있다는 느낌이 들어도 쓰지 마라.** 그 느낌이 곧 환각이다.
  코드 체계와 과목명만으로 문장을 복원할 수 있다고 생각하는 순간이 가장 위험하다.
- 사용자에게 "코드는 실재하나 이 데이터셋에 진술문이 없다 — 원문 고시본 확인 필요"라고 알린다.
- 대안이 필요하면 `--keyword`로 같은 과목의 **실재하는** 다른 성취기준을 제시한다.

## 신뢰 등급을 읽어라

모든 레코드는 `statement_verified`를 갖는다. 별책 고시본과 성취수준 문서는 서로 독립적으로
만들어져 같은 성취기준 문장을 각각 싣기 때문에 대조 검산이 가능하다.

| `statement_verified` | 건수 | 뜻 | 인용할 때 |
|---|---:|---|---|
| `true` | 2849 | 두 출처가 **글자까지 같은** 문장을 싣는다 | 그대로 인용 |
| `false` | 252 | 두 출처가 **다른** 문장을 싣는다 — 한쪽이 손상됐고 어느 쪽이 고시 원문인지 이 데이터로는 판정 불가 | 인용하되 "원문 대조 필요"를 함께 밝혀라 |
| `null` | 193 | 대조할 독립 출처가 없었다(154) + 진술문 자체가 없다(39) | 단일 출처임을 밝혀라 |

`true`도 "고시 원문과 같다"는 보장은 아니다. 두 문서가 같은 오류를 물려받은 자리는 이 방법으로
잡히지 않는다. 공문서·평가계획처럼 정확성이 곧 책임인 문서라면 최종 확인은 고시 원문이다.

**`needs_review: true` 391건(11.9%)** — 사람이 봐야 하는 레코드. 이 중 **278건이 제2외국어**
하나에 몰려 있다(별책16의 PDF 변환 품질이 눈에 띄게 나쁘다). 제2외국어를 쓸 때는 기본적으로
원문 확인을 권하라. 나머지 13개 교과는 2723건 중 113건(4.1%)이다.

진술문 안에 라틴 문자 덩어리가 박혀 있거나(`다양한 의사소통 HAS 듣고 …`) 말이 끊긴 자리는
OCR 잔재(50건)다. **임의로 고쳐서 인용하지 마라.** 그건 verbatim이 아니라 창작이다.
손상을 그대로 인용하기도 곤란하니, 그 성취기준은 원문 고시본을 확인해야 한다고 알려라.

## 「없다」의 두 가지 의미

조회 결과가 비었을 때 **"그런 성취기준은 없습니다"라고 단정하기 전에** 어느 쪽인지 가려라.

1. **정말 없는 코드** — 지어낸 코드이거나 형식만 그럴듯한 코드. 그냥 없다고 답하고
   `--keyword`로 실재하는 대안을 찾아 제시한다.
2. **원문 손상으로 미수록** — 번호 순서상 존재해야 하는데 원문 PDF에서 코드 마커가 통째로
   파괴돼 추출되지 않은 자리가 **26개** 있다(실증: 별책16의 `12독문01-03`은 원본 파일 어디에도
   문자열로 존재하지 않는다). 이 성취기준은 고시에는 있고 이 데이터셋에만 없다.
3. **이 데이터셋이 다루지 않는 과목** — 아래 「범위 밖」 참조.

판별법: `validation-report.md`의 **「번호 시퀀스 공백」**(26개)과 **「수록하지 않은 코드 전체
목록」**을 확인한다. 거기 있으면 "존재하지 않는다"가 아니라 "원문 손상/범위 밖으로 미수록"이므로,
없다고 단정하지 말고 원문 고시본 확인을 안내하라. 두 경우를 뭉뚱그리는 것도 거짓말이다.

**안내할 곳**: 교육부·한국교육과정평가원이 운영하는 **KICE 학생평가지원포털**
(https://stas.moe.go.kr/cmn/main). 로그인 없이 메인 화면 `성취기준` 탭에서
학교급 → 학년군 → 교과 → 과목 순으로 조회한다. PDF를 거치지 않은 원본 텍스트라 이 데이터셋이
겪은 변환 손상이 없고, 2015·2009 개정과 영역(단원)·해설·평가기준도 제공한다.
위 2·3번에 해당하거나, 레코드가 `needs_review` 또는 `statement_verified: false`인 자리에서는
**사용자에게 이 포털을 함께 안내하라.** 다만 포털을 직접 조회할 수는 없으니
그 내용을 아는 척 지어내지 말고, 링크와 조회 방법만 알려라.

## 범위 밖 (여기에는 답이 없다)

- **특성화고 전문교과(NCS)** — `[성직 01-01]` 형식. 별도 코드 체계이며 미수록.
- **과학·체육·예술 계열 전문 교과(과학고·체고·예고)와 고등학교 교양 교과** — 해당 별책
  고시본이 원자료에 없어 성취기준 문장을 원문으로 확인할 수 없었다. 성취수준 문서에서만
  코드가 보이는 것이 1141개인데 그중 **1095개**가 여기 해당한다(정보·보건·환경·연극·영화·
  사진·무용·문예 창작 등). 확인할 수 없는 문장은 싣지 않기로 한 결과다.
- 나머지 **46개**는 이 데이터셋이 다루는 과목인데 그 코드만 빠졌다 — 위 2번(원문 손상)에 해당.
- 전체 목록은 `validation-report.md`의 「수록하지 않은 코드 전체 목록」.

범위 밖 과목을 요청받으면 **"이 데이터셋에는 없다"고 답하고 원문 고시본·NCIC를 안내하라.**
범위 밖이라는 말은 "네 기억으로 채워도 된다"는 뜻이 아니다. 데이터가 없는 자리야말로
지어낸 답이 검증 없이 통과하는 자리다.

## 검증

```bash
python3 scripts/verify.py 수업지도안.md --school 중
cat 문서.md | python3 scripts/verify.py -
```

지어낸 코드 한 줄과 진술문을 손본 한 줄을 넣은 문서를 검사한 실제 출력:

```
FAKE  [9수01-99] — 이 데이터셋에 없는 코드. 지어낸 코드이거나, 이 데이터셋이 다루지 않는 과목(전문 교과·교양 등)이다. 원문 고시본으로 확인하기 전에는 인용하지 마라
MISMATCH [9수01-02] — 진술문이 원문과 다름
  원문: 소인수분해를 이용하여 최대공약수와 최소공배수를 구할 수 있다.

문제 2건 발견 (검사 코드 3개)
```

`verify.py`가 낼 수 있는 표시는 아래 여덟 가지가 전부다.

| 표시 | 뜻 | exit code 영향 |
|---|---|---|
| `FAKE` | 데이터셋에 없는 코드 | 문제 |
| `MISMATCH` | 코드는 맞지만 진술문이 원문과 다름 | 문제 |
| `NOSTMT` | 코드는 실재하나 진술문 미수록이라 인용을 검증할 수 없음 | 문제 |
| `LEVEL` | `--school`과 성취기준의 **학교급** 불일치 (성취수준과 무관하다) | 문제 |
| `LVLDIFF` | `A수준:` 뒤 서술문이 그 등급의 원문과 다름 (다른 등급의 서술문이면 어느 등급인지 함께 알린다) | 문제 |
| `LVLMISS` | 그 성취기준에 없는 등급을 인용 — 수록된 등급 목록을 함께 알린다 | 문제 |
| `WARN` | 인용은 데이터셋과 일치하나 그 원문이 미검증(`statement_verified`가 `true`가 아님) | 경고만 |
| `LVLNONE` | 성취수준이 수록되지 않은 성취기준의 등급을 인용 — 검증할 방법이 없음 | 경고만 |

`LEVEL`은 학교급, `LVL*`은 성취수준이다. 이름이 닮았을 뿐 다른 검사다.

문제 0건이면 exit 0, 1건 이상이면 exit 1이라 CI 게이트로 쓸 수 있다. `WARN`과 `LVLNONE`은
인용자의 잘못이 아니라 데이터의 한계라 exit code를 바꾸지 않는다 — 그래도 사용자에게는 전달하라.

### 코드 바로 뒤 한 칸은 원문 자리다

**인용한 코드 바로 뒤에 오는 문장은 그 성취기준의 원문이어야 한다.** 그 자리에 원문 아닌
완결된 서술문(`~다`로 끝나는 문장)을 적으면 `MISMATCH`가 난다. 닮았는지와 무관하다.

```
$ echo '[9수01-01] 실생활 사례를 활용하여 개념을 도입한다.' | python3 scripts/verify.py -
MISMATCH [9수01-01] — 코드 바로 뒤가 원문이 아닌 문장이다(2015 개정 진술문이거나 지어낸 문장). 인용이라면 글자까지 원문과 같아야 하고, 지도 메모라면 코드에 붙이지 말고 줄을 나눠 적어라
  원문: 소인수분해의 뜻을 알고, 자연수를 소인수분해 할 수 있다.

문제 1건 발견 (검사 코드 1개)
```

**일부러 이렇게 만들었다.** 2015 개정 진술문은 이 데이터셋에 없어서 원문과 대조할 수가
없다. 코드 옆자리를 원문 전용으로 예약해 두는 것이 2015 개정 혼입과 지어낸 문장을 잡는
유일한 방법이다. 대신 그 자리에 지도 메모를 적은 문서가 무고당한다 — 아래 중 하나로 피한다.

- **줄을 나눈다.** 이 판정은 코드와 **같은 줄**만 보므로, 메모를 다음 줄로 내리면 통과한다.
  ```
  [9수01-01] 소인수분해의 뜻을 알고, 자연수를 소인수분해 할 수 있다.
  실생활 사례를 활용하여 개념을 도입한다.
  ```
- **수업 운영 낱말을 쓴다.** `성취기준·성취수준·차시·단원·지도안·교사·학생·모둠·교과서·
  학습지·활동지·학습목표·평가계획·지필평가·수행평가·형성평가·총괄평가·채점·루브릭·
  피드백·유의점·준비물` 중 하나가 든 문장은 인용이 아니라 성취기준을 놓고 쓴 산문으로
  본다. `[9수01-01] 학생들이 흥미를 느끼도록 실생활 사례를 활용한다.`는 통과한다.
  (진술문이 있는 3255건 중 이 낱말이 든 것은 `학생` 1건뿐이다 — 그래서 구분자가 된다.)
- **코드에 조사를 붙인다.** `[9수01-01]을 중심으로 차시를 구성하였다.`처럼 코드가 문장의
  일부이면 인용 형식이 아니므로 통과한다.

가장 안전한 순서는 **원문을 먼저 verbatim으로 인용하고, 해설·요약·메모는 그 다음 줄부터**다
(규칙 2와 같은 말이다).

**`OK`가 보장하지 않는 것.** `verify.py`는 문서에서 찾아낸 코드가 실재하는지, 코드 옆에
인용된 문장이 원문과 같은지, `A수준:` 자리의 서술문이 그 등급의 원문과 같은지를 본다.
그 밖은 잡지 못한다 — `OK`를 "이 문서는 정확하다"로 읽지 마라.

- **교과 오인.** 충돌 코드 10개는 어느 교과를 의도했는지 문서만 보고 알 수 없어, 인용문과
  가장 가까운 레코드로 판정한다. 문장 없이 코드만 적었다면 잘못된 교과라도 `OK`가 나온다.
- **주제 부적합.** 실재하는 코드지만 그 수업·평가에 맞지 않는 성취기준인 경우.
- **문장 없는 인용.** 코드만 적고 진술문을 안 썼으면 실재 여부만 확인된다.
- **등급을 밝히지 않은 성취수준 인용.** 성취수준 검사는 `A수준:`처럼 등급과 콜론이 있는
  줄에만 걸린다. 등급 표시 없이 서술문만 옮겨 적으면 검사 대상이 아니다.
- **성취수준의 고시 원문 대조.** `LVLDIFF`가 없다는 것은 이 데이터셋의 `levels`와 같다는
  뜻이지 고시 원문과 같다는 뜻이 아니다. 등급 배정이 의심스러운 56건은 여전히 통과한다.

## 코드 해부 (상세: `references/code-system.md`)

- `[9수01-01]` = 중학교(9) · 수학(수) · 1영역(01) · 1번(01)
- 학년군 접두: `2`=초1-2 · `4`=초3-4 · `6`=초5-6 · `9`=중1-3 · `10`=고 공통 · `12`=고 선택
- 고교 공통과목은 3단: `[10공수1-01-01]` = 공통수학1 · 1영역 · 1번
- 로마숫자 꼬리(`12영Ⅰ-01-01`)와 괄호 약어(`9사(일사)01-01`)도 실재하는 표기다
- 접두어→과목명 전체 표(193개): `references/code-system.md`, 원본은 `index/subjects.json`

## 함정

- **2015 개정 혼입.** 2015 개정도 `[9수01-01]` 같은 형식을 쓴다. 코드가 실재하는데 진술문이
  다르면 2015 개정 자료일 가능성이 크다 — `verify.py`의 `MISMATCH`가 이 경우를 잡는다.
  이 스킬은 **2022 개정 전용**이며, 2015 개정 진술문은 여기 없다. 대조할 원문이 없는데도
  잡아낼 수 있는 이유가 [코드 바로 뒤 한 칸은 원문 자리다](#코드-바로-뒤-한-칸은-원문-자리다)이다.
- 진술문을 성취수준 문서에서 가져온 레코드가 152건이다(별책 쪽이 손상된 자리).
  `statement_source` 필드로 구분된다.
- `commentary_only: true` 23건은 별책 본문 목록이 아니라 성취기준 해설에서만 코드가 확인된 것이다.

## 파일 지도

| 경로 | 내용 |
|---|---|
| `data/index.json` | 36개 데이터 파일 목록과 건수, 총계 |
| `data/<학교급>/<교과>.jsonl` | 성취기준 레코드 (1줄 1건) |
| `index/subjects.json` | 접두어·약어 → 과목명 193건 |
| `scripts/lookup.py` | 조회 CLI |
| `scripts/verify.py` | 문서 인용 검증 CLI |
| `validation-report.md` | 교차검증 결과·번호 공백·미수록 코드 전체 목록 |
| `references/code-system.md` | 코드 체계와 과목 약어 전체 표 |
| `references/sources.md` | 출처 고시·발행처·제외 자료와 사유 |

레코드 필드: `code` `subject` `statement` `source` `statement_source` `commentary_only`
`needs_review` `school` `grade_band` `course` `area` `levels` `statement_verified`
